machine:
  node:
    version: 7.2.0

  post:
    - curl -o- -L https://yarnpkg.com/install.sh | bash

dependencies:
  pre:
    - yarn --version

  override:
    - yarn install

compile:
  override:
    - yarn build
